let createServer;_9ba‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);

const server = createServer((request, response) => {
    switch (request.url) {
        case '/status': {
            response.wiriteHead(200);
            response.write('Okay');
            response.end()
            return;
        }
    }
});

server.listen(8000, '127.0.0.1', () => {
    console.log('Server is listening at http://127.0.0.1:8000');
});